import Header from '@/components/layout/Header';

export default function CreateTeamPage() {
  return (
    // 추후 수정: 확인용으로 임의로 작성됨.
    <>
      <Header />
      <main className="ml-[315px] min-h-[calc(100vh-90px)] bg-background px-10 py-8 pt-[calc(90px+32px)] text-foreground">
        CreateTeamPage
      </main>
    </>
  );
}
