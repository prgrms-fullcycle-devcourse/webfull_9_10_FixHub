{
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["prisma:generate"]
    },
    "build": {
      "dependsOn": ["^build", "prisma:generate"],
      "outputs": ["dist/**", "build/**"]
    },
    "prisma:generate": {
      "cache": false
    },
    "lint": {},
    "format": {}
  }
}
